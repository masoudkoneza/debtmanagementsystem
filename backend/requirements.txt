# DMS Backend — Python dependencies
#
# Pin minor versions to avoid surprise breakage on fresh installs.
# Update these periodically and re-run the test suite before committing.

Django==4.2.*
djangorestframework==3.15.*
djangorestframework-simplejwt==5.3.*
django-cors-headers==4.3.*
psycopg2-binary==2.9.*      # PostgreSQL adapter (binary for easy Docker builds)
dj-database-url==2.1.*      # Parse DATABASE_URL env var into Django DATABASES dict
